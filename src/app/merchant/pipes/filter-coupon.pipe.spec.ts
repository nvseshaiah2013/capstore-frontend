import { FilterCouponPipe } from './filter-coupon.pipe';

describe('FilterCouponPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCouponPipe();
    expect(pipe).toBeTruthy();
  });
});
